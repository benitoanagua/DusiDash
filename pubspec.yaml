name: dusidash
description: "Dashboard with Fluent UI"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  fluent_ui: ^4.1.0
  provider: ^6.1.1
  go_router: ^14.0.2
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
